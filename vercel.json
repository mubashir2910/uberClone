{
  "version": 2,
  "builds": [
    { 
      "src": "BACKEND/app.js", 
      "use": "@vercel/node" 
    },
    { 
      "src": "FRONTEND/package.json", 
      "use": "@vercel/static-build",
      "config": { 
        "distDir": "dist"
      } 
    }
  ],
  "routes": [
    { "src": "/api/users/(.*)", "dest": "/BACKEND/app.js" },
    { "src": "/api/captains/(.*)", "dest": "/BACKEND/app.js" },
    { "src": "/api/rides/(.*)", "dest": "/BACKEND/app.js" },
    { "src": "/api/maps/(.*)", "dest": "/BACKEND/app.js" },
    { "src": "/(.*)", "dest": "/FRONTEND/dist/$1" }
  ]
}